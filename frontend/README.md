# Student Task Manager - Frontend

React + Vite + React Router + Axios + Context API

## Setup

```bash
npm install
npm run dev
```

App run at http://localhost:5173

Backend must run at http://127.0.0.1:8000 (see backend README).
